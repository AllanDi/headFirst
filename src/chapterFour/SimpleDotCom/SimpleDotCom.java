package chapterFour.SimpleDotCom;

public class SimpleDotCom {

    -->declarar uma matriz de inteiros para armazenas o local das celulas, (locationCells)
    -->declarar um inteiro que armazena o numero de palpites (numOfHits) iniciando em 0

    -->declarar um metodo (checkYourSelf que use a string para o palpite do usuario, verificar a string e
                        retornar (acerto), (erro) ou eliminado

    -->declarar um metodo de configuração (setLocationCells) que use a matriz int contendo os tres locais
    das celulas na forma de numeros inteiros gerados aleatiamentes






}
